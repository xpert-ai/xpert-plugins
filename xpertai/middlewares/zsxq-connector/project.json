{
  "name": "@xpert-ai/plugin-zsxq-connector",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "middlewares/zsxq-connector/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "middlewares/zsxq-connector/dist",
        "main": "middlewares/zsxq-connector/src/index.ts",
        "rootDir": "middlewares/zsxq-connector/src",
        "tsConfig": "middlewares/zsxq-connector/tsconfig.lib.json",
        "generatePackageJson": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "middlewares/zsxq-connector",
        "command": "tsc -p tsconfig.spec.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "middlewares/zsxq-connector",
        "command": "jest --config jest.config.ts --runInBand"
      }
    }
  }
}
