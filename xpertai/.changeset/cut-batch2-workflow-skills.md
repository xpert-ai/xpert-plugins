---
"@xpert-ai/plugin-cut": patch
---

Split Cut guidance into basics, speech editing, captions, verification, and export skills, and reuse the packaged workflows in Assistant templates and MCP prompts. Reuse existing content approval for identical edits while preserving platform confirmation and revision checks.

Add a portable Xpert Cut Agent Plugin entry and a standalone client package builder that reuses the same Skills and connects to a selected remote Cut MCP service, while preserving the Xpert runtime installation entry.
