---
"@xpert-ai/plugin-pdfium": minor
---

Add PDFium knowledge document parsing with extracted text, page images and page metadata. Share the bounded worker with the existing PDF tool and allocate independent output directories so duplicate filenames and concurrent conversions preserve every result.

Require plugin SDK 3.15.18 or later, matching the validated parser contract.
