---
'@xpert-ai/plugin-file-memory': minor
---

Add workbench-facing file APIs for file-memory and fix hosted request-context resolution.

This update lets hosts browse and edit memory files through the shared file workbench flow while keeping managed `MEMORY.md` files protected from direct edits.
