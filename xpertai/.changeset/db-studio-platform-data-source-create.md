---
'@xpert-ai/plugin-db-studio': patch
---

Use the generic `platform.data-source.create` host command to create connections. Requires a host exposing this command; update the host and plugin together.
