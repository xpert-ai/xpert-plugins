---
"@xpert-ai/plugin-cut": patch
---

Add a portable Xpert Cut Agent Plugin entry and a standalone client package builder that reuses the shared Skills and connects to a selected remote Cut MCP service, while preserving the Xpert runtime installation entry.

Add an interactive Codex installer with hidden API key input, authenticated MCP discovery, a private local plugin source, and updates that preserve connection settings.
