---
"@xpert-ai/plugin-jieba": minor
---

Add a standalone Jieba keyword analyzer plugin for organization-scoped knowledgebases. Use pinned search segmentation and dictionaries with identical index/query normalization. Requires the keyword analyzer extensions in SDK 3.19.0 and the corresponding host implementation.
