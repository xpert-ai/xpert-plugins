---
'@xpert-ai/plugin-amap-connector': patch
---

Add the organization-scoped AMap connector with platform-vault credentials, optional digital signing, 13 bounded read-only Agent tools, official Web Service endpoint mappings, and allowlisted GCJ-02 response DTOs.
