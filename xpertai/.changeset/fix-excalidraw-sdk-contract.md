---
"@xpert-ai/plugin-excalidraw": patch
---

Require SDK and contracts 3.18.4 for the tool result and MCP approval APIs used by Excalidraw.

Synchronize the portable plugin manifest version before packing so Changesets releases pass metadata validation.
