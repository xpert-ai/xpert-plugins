---
'@xpert-ai/plugin-presentation-studio': minor
---

Route PDF and PPTX exports through the shared Xpert Browser Runtime and a versioned `presentation.export` Sandbox Action Bundle while retaining the browser-free HTML path. Exports are enabled by default and surface Action, Definition, Runtime Binding, Provider artifact, and worker health warnings without a tenant feature switch.
