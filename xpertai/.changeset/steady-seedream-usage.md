---
'@xpert-ai/plugin-volcengine': patch
---

Run Seedream image generation through the host IMAGE model client, preserve Provider token usage alongside generation usage, publish per-generation pricing rules for models with official prices, and retain the Tool usage callback as a compatibility fallback.
