{
  "name": "@xpert-ai/plugin-notion-connector",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "connectors/notion-connector/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "connectors/notion-connector/dist",
        "main": "connectors/notion-connector/src/index.ts",
        "rootDir": "connectors/notion-connector/src",
        "tsConfig": "connectors/notion-connector/tsconfig.lib.json",
        "generatePackageJson": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "connectors/notion-connector",
        "command": "tsc -p tsconfig.spec.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "connectors/notion-connector",
        "command": "jest --config jest.config.cjs --runInBand"
      }
    }
  }
}
