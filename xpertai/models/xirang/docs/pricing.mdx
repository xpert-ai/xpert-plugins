---
title: 费用计算
description: 天翼云星辰价格解析和未定价保护
---

## 规则

精确的 `¥ N/百万Tokens` 会生成 input/output CNY 规则，单位大小为 1,000,000。模型详情提供缓存命中输入价时，插件会额外生成 `cache_read_input` 规则；缓存命中 Token 是输入 Token 的子集，不会重复计入总 Token。标准时段按 Asia/Shanghai 08:00–24:00，优惠时段按 00:00–08:00。

当前已开通模型采用以下账户价格：

请求中的 `model` 参数可以使用模型名称或模型 ID。

| 模型 | 模型 ID | 输入 | 缓存命中输入 | 输出 |
| --- | --- | ---: | ---: | ---: |
| `glm-5.3` | `e8e2511658054053a7e56e950d80f0e4` | ¥8/百万 Tokens | ¥2/百万 Tokens | ¥28/百万 Tokens |
| `deepseek-v4-flash-0731-new` | `9570194f36ab4ac3bb31e58ea839950b` | ¥3/百万 Tokens | ¥0.3/百万 Tokens | ¥9/百万 Tokens |

服务页面使用 `~` 表示区间价，但没有展示对应的 token 阈值。插件不会猜测阈值，而是通过不匹配的哨兵规则让账本标记为 `unpriced`。`-` 和“免费试用”同样不代表永久免费。

图片模型使用 `generation` 用量。当前只有 Seedream 4.5 的模型详情确认了 CNY 0.25/张；其余图片模型继续显示为未定价。

价格页面可能随账户、活动或时间变化。更新快照后必须重新生成、测试和发布插件，并保留旧版本规则以便账单追溯。
