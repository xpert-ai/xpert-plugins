# 测试清单

## ✅ 已完成

- [x] 创建 test 文件夹
- [x] 创建配置文件 `config.ts`（用于填入 API Key）
- [x] 创建配置示例文件 `config.example.ts`
- [x] 移动测试文件到 test 文件夹
- [x] 更新测试文件以使用配置文件
- [x] 配置 .gitignore 确保 config.ts 不被提交
- [x] 更新 tsconfig.spec.json 包含 test 文件夹

## 📝 待完成

### 第一步：配置 API Key

1. 打开 `test/config.ts` 文件
2. 在 `apiKey` 字段填入你的 DeepSeek API Key：
   ```typescript
   apiKey: 'sk-xxxxxxxxxxxxx', // 👈 在这里填入
   ```

### 第二步：运行测试

```bash
# 从 deepseek 插件目录运行
npx tsx test/test-developer-role-fix-simple.ts
```

## 📁 文件结构

```
test/
├── config.ts                    # ✅ 配置文件（填入 API Key）
├── config.example.ts            # ✅ 配置示例
├── test-developer-role-fix-simple.ts  # ✅ 简单测试脚本
├── test-developer-role-fix.test.ts    # ✅ Jest 单元测试
├── README.md                    # ✅ 详细说明
├── 使用说明.md                  # ✅ 中文使用说明
└── .gitignore                   # ✅ 忽略 config.ts
```

## 🔍 验证步骤

1. **确认配置文件存在**
   ```bash
   ls test/config.ts
   ```

2. **确认已填入 API Key**
   ```bash
   # 检查 config.ts 中 apiKey 不为空
   grep "apiKey:" test/config.ts
   ```

3. **运行测试**
   ```bash
   npx tsx test/test-developer-role-fix-simple.ts
   ```

## 🎯 测试目标

验证 `deepseek-reasoner` 模型：
- ✅ 不会出现 `developer` role 错误
- ✅ 可以正常处理 system message
- ✅ 可以正常进行多轮对话
- ✅ 可以正常使用流式响应

