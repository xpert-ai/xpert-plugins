import { pluginOpendataloader } from './plugin-opendataloader.js'

describe('pluginOpendataloader', () => {
  it('should work', () => {
    expect(pluginOpendataloader()).toEqual('plugin-opendataloader')
  })
})
