interface:
  display_name: "Excalidraw Agent Skill"
  short_description: "Use Excalidraw middleware tools with an elements-first workflow for drawing creation, selection-aware edits, scene inspection, patching, versioning, Mermaid drafts, and recovery."
  default_prompt: "Create an editable architecture diagram in Excalidraw"
policy:
  allow_implicit_invocation: true
