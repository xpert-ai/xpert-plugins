spec_version: 1
id: pixel-transition
name: Pixel Transition
description: A card flips to a second face through a burst of pixel tiles — a retro reveal on hover.
surfaces: [web, interaction]
canvas: [web]
target: [card,image]
intent: hover-press
runtime: [css,js]
export: [skill, motion-json, html]
category: hover-press
tech: [css,js]
dependencies: []
tags: [pixel transition,pixelate reveal,像素转场,方块揭示]
intent_keywords: [pixel transition,pixelate reveal,像素转场,方块揭示]
best_for: ["Pixel Transition — one tasteful use"]
avoid_when: ["Overuse; dense grids; reduced-motion contexts where it distracts"]
restraint: { max_per_view: 3, notes: "Use sparingly. Plain/static under reduced-motion." }
motion: { duration_ms: 600, easing: ease-out, reduced_motion: graceful, gpu_safe: true }
entry: preview.html
implementations:
  - { tech: css,js, files: [pixel-transition.css,pixel-transition.js], usage: "See preview.html; add the class." }
license: { spdx: Apache-2.0, upstream: https://reactbits.dev/animations/pixel-transition, attribution_required: false }
author: { name: nexu.io, url: "https://github.com/nexu-io" }
version: 0.1.0
