spec_version: 1
id: dock
name: Magnify Dock
description: A macOS-style dock whose icons magnify toward the cursor — a delightful app-launcher row.
surfaces: [web, interaction]
canvas: [web]
target: [list,button]
intent: hover-press
runtime: [css,js]
export: [skill, motion-json, html]
category: hover-press
tech: [css,js]
dependencies: []
tags: [dock,magnify,macos dock,程序坞,放大停靠栏]
intent_keywords: [dock,magnify,macos dock,程序坞,放大停靠栏]
best_for: ["Magnify Dock — one tasteful use"]
avoid_when: ["Overuse; touch-only where relevant; reduced-motion where it distracts"]
restraint: { max_per_view: 2, notes: "Use sparingly. Plain under reduced-motion." }
motion: { duration_ms: 300, easing: ease-out, reduced_motion: none, gpu_safe: true }
entry: preview.html
implementations:
  - { tech: css,js, files: [dock.css,dock.js], usage: "See preview.html; add the class." }
license: { spdx: Apache-2.0, upstream: https://reactbits.dev/components/dock, attribution_required: false }
author: { name: nexu.io, url: "https://github.com/nexu-io" }
version: 0.1.0
