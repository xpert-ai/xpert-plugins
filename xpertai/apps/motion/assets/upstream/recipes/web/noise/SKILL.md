---
name: noise
description: |
  A film-grain noise overlay — subtle animated texture over any background. A marketing-grade hero backdrop. One per view; scrim behind text.
  Renders a static frame under prefers-reduced-motion.
triggers: ["noise","animated background","动态背景"]
od: { mode: prototype, surface: web, platform: desktop, category: animation-motion, preview: { type: html }, design_system: { requires: false } }
---
# noise
A film-grain noise overlay — subtle animated texture over any background.
## How to apply
Copy `noise.js`; add `<div class="noise" style="position:absolute;inset:0"></div>` behind content.
