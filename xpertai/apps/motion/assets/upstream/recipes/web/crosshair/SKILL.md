---
name: crosshair
description: |
  A thin crosshair spans the viewport and tracks the pointer — a precise, techy custom cursor. One per page; suits tools/dashboards. Off on touch and under prefers-reduced-motion.
triggers: ["crosshair cursor","targeting lines","准星光标","十字定位"]
od: { mode: prototype, surface: web, platform: desktop, category: animation-motion, preview: { type: html }, design_system: { requires: false } }
---
# crosshair
Crosshair Cursor Dependency-free, reduced-motion safe.
## How to apply
Copy `crosshair.js` and include it. Set `window.__chColor` before load to tint.
