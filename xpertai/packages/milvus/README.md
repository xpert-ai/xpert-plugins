# milvus

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build milvus` to build the library.

## Running unit tests

Run `nx test milvus` to execute the unit tests via [Jest](https://jestjs.io).
