# ragflow

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ragflow` to build the library.

## Running unit tests

Run `nx test ragflow` to execute the unit tests via [Jest](https://jestjs.io).
