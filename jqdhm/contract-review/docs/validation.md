# 验证记录

## 本地验证

| 项目 | 结果 |
| --- | --- |
| TypeScript 类型检查 | 通过 |
| TypeScript 适配层测试 | 9/9 通过 |
| 工作台页面测试 | 6/6 通过 |
| Java Spring Boot 测试 | 17/17 通过，使用 JDK 21 |
| Java 服务打包 | 通过 |
| 插件生命周期加载 | 通过 |
| npm 打包检查 | 通过，未包含 `node_modules`、`target`、`.env` |
| 本地 Demo 健康检查 | Java `/health` 和预览页面均返回 HTTP 200 |

## 验证边界

当前没有可安装的 Xpert 环境，因此尚未完成真实平台安装、真实模型调用和线上部署验收。`examples/draft.json` 是虚构测试数据，只替代模型提取候选这一步；它不代表真实模型结果。

完整平台验收需要配置 Xpert 地址、插件安装权限、支持工具调用的模型及额度，并让 Xpert 宿主可以访问 Java 服务。服务 Token 只放在本地或部署环境变量中，不提交到 Git。
