# 部署到测试平台所需的环境变量示例（脱敏，不含真实密钥）
# 复制为部署终端的环境变量即可；实际值由平台/测试账号提供，切勿提交真实凭证。

# 平台 API 源（部署 --api-url 用）
XPERT_API_URL="https://<test-host-origin>"

# 插件安装范围：需匹配插件声明 level=tenant，提供对应租户/组织标识
XPERT_PLUGIN_SCOPE="<tenant-or-organization-id>"

# 有插件安装权限的测试账号（示例占位，勿用真实值）
XPERT_ADMIN_TOKEN="<paste-token-with-plugin-install-scope>"

# 模型凭证由平台“模型管理”配置，插件本身不读取任何密钥，也不需要在此声明。
