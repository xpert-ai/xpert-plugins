# 验证记录与截图缺口

2026-09-14 提交前重新执行 0.3.0 构建、67 项测试、类型检查和生命周期检查，均通过。平台与模型部分沿用此前回执；本次未操作浏览器或重启服务。不同层次的结果不能互相替代。

## 四层验证

| 层次 | 已有结果 | 边界与待补项 |
| --- | --- | --- |
| 单元/构建 | 0.3.0 上游 56 项、插件 11 项通过；最终 HTML/模板匹配 | 不证明所有真实鼠标交互 |
| 插件生命周期 | 本次 0.3.0 从 dist 加载及关闭成功 | 使用内置 mocks，不证明真实数据库和平台流程 |
| 界面集成 | 0.3.0 DOM 回归验证选区、文件全文、行号、菜单排除与事件清理 | 未进行最终浏览器验收 |
| 平台业务流程 | 0.3.0 部署、重启、原模板更新、运行资源回读和真实模型解释通过 | 人工确认保存恢复、失败重试完整流程待验证 |

另有宿主 PR：前端命令测试 16 项通过，覆盖真实未挂载控制器拒绝；服务端 human-input 5 项通过、2 项原有元素文案断言失败，新增标签测试通过。不是本插件的 67 项测试，不应合并后声称全部绿。

生命周期复核命令（先按 harness 自己的 README 准备依赖和构建）：

```sh
node plugin-dev-harness/dist/index.js --workspace ./community/apps/dockyard --plugin @community/apps-dockyard
```

## 当前真实模型证据

通过 ChatKit 使用的运行 API 传入合成 example.js 引用，模型解释 `tax(amount) { return amount * 0.2 }`，正确回答 100 对应 20。临时验收会话已清理。完整合成输入对应的输出摘录见 [脱敏证据](evidence.json)。这是接口到模型证据，不是完整界面或修改保存证据。

## 必须补齐的实际截图

按面试说明，截图最终应放在插件 README，图片随插件提交并使用相对路径。当前不放占位图，也不以旧原型或上游截图冒充运行截图。

| 截图 | 应证明什么 | 状态 |
| --- | --- | --- |
| 当前工作台与版本 | 插件在 Xpert 运行，两个右键入口 | 待最终版截图 |
| 用户输入 | 实际引用内容、范围、问题 | 待最终版截图 |
| AI 处理结果 | 当前业务输入对应的真实回答 | 接口结果有证据，界面截图待补 |
| 异常与重试 | 明确错误、纠正/重试后的结果，无重复业务结果 | 待真实流程验证和截图 |
| 保存与恢复 | 采用结果、保存、刷新后仍存在 | 待完整路线验证和截图 |

此前用户提供的截图展示过旧版浮窗、加载与失败情况，可作为调试历史，不能替代 0.3.0 的最终运行截图。本次不自动打开浏览器。

## 待执行的最短演示脚本

打开一个文本文件 → 右键解释 → 输入明确问题 → 查看真实回答 → 选择一项修改目标 → 手工采用模型建议 → 保存 → 刷新确认；另演示一次引用未就绪或模型失败后的纠正与重试。每一步记录预期、实际和截图，未完成前标记为待验收。
