# Documentation maintenance

Keep descriptions in Chinese and align them with the implemented public contracts and verified UI. Keep Mock and real platform execution clearly identified. Update navigation in docs.json when adding pages; run mint validate before release.
