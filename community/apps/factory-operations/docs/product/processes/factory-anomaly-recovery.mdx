---
title: 工厂异常恢复流程
description: 从异常检测到恢复验证的版本化业务流程。
---

流程依次完成异常检测、研判、四域并行分析、恢复方案生成、人工决策、受控执行和恢复验证。每次写入携带 `caseId`、`baseRevision` 与幂等 `operationId`；路由只依据服务端持久化事实，不能依据自然语言猜测。

设备、质量、生产和资源分析在研判后并行。恢复方案必须等待四域结果。审批绑定 Case 修订号；执行需要明确批准；验证必须检查每个动作的持久化确认。任何拒绝、部分失败或恢复未满足都会进入人工干预终点。

